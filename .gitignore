# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode build output
build/
DerivedData/
*.xcarchive

# Release artifacts (DMGs and staging; uploaded to GitHub Releases, not committed)
dist/

# Generated tool output (regenerate with tools/audit_registry.py)
tools/registry_audit.csv

# Compiled diagnostic tool (build with the clang line in its header comment)
tools/scroll_event_capture

# Xcode user-specific settings (editor state, breakpoints, etc.)
xcuserdata/
*.xcuserstate
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/

# Python bytecode cache
__pycache__/

# Playgrounds
timeline.xctimeline
playground.xcworkspace
.claude

# AI session files (local use only, not for public repo)
CLAUDE.md
progress.md
Notes/Scratch/
swift-*
